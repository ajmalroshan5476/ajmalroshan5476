{
  "name": "creator-collaboration-platform",
  "version": "1.0.0",
  "description": "A collaboration platform for video editors and content creators",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["collaboration", "video-editing", "content-creation", "chat", "file-sharing"],
  "author": "Creator Platform",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}